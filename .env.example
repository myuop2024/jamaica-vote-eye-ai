
# IMPORTANT: This is an example file. Copy this to a new file named .env
# and fill in your actual configuration values.
# Ensure all critical variables like JWT_SECRET, VITE_CHAT_ENCRYPTION_KEY,
# VITE_SUPABASE_URL, and VITE_SUPABASE_ANON_KEY are correctly set for the
# application to function properly, especially the chat and file upload features.
# For production, these should be secure, unique values.

# ... existing environment variables ...
# Chat System
VITE_CHAT_ENCRYPTION_KEY=your_chat_encryption_key_here
JWT_SECRET=your_jwt_secret_here
# ... existing code ... 
# Supabase Configuration (Required for File Uploads in Chat)
VITE_SUPABASE_URL=your_supabase_url_here
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here
